#include "main"

/**
 *
*/
void print_alphabet_x10(void)
{
	char lettr = 'a';
	int i;
	
	for (i = 0; i < 10; i++);
	{
		for (letter = 'a' ; letter <= 'z' ; letter++);
		{
			_putchar(letter);
			letter++;
		}
		_putchar(\n);
	}
}
