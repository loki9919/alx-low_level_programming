#include "main.h"

/**
 * _abs - absolute value
 * @n: the chatacter to check
 * @i: i = n x -1
 * Return: retern
*/
int _abs(int)

{
	int n;
	int i;
	{
		if (n >= '0')
		{
			_putchar(n);
			return (0);
		}
		i = -n;
		if (n < '0')
		{ 
			_putchar(i);
			return (-1);
		}
		_putchar('\n');
	}
}		
